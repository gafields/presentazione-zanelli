export function somma(a, b) {
    return a + b;
}
// export function divisione(
//   a: number,
//   b: number,
//   inverti: boolean = false,
// ): number {
//   if (b === 0 || (a === 0 && inverti)) return Number.NaN;
//   if (inverti) {
//     return b / a;
//   }
//   return a / b;
// }
